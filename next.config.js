/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  
    images: {
        domains: ["localhost", "techshubham.website"],
      },
     
 


}

module.exports = nextConfig
